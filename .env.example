# Copy to .env (gitignored) and fill in. Used only by scripts/ at build time —
# never imported from src/, so these never reach the browser.
#
# Free account + token: https://wigle.net → Account → Show my token
WIGLE_API_NAME=
WIGLE_API_TOKEN=
